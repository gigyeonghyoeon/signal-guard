# Signal Guard — 실시간 교통신호 상태정보 오류검지 시스템

> 수업용 MVP 범위: **인천시** 교차로 데이터로 검증 가능한 웹 기반 모니터링 플랫폼  
> 근거: 한국도로교통공단 제안요청서(2025.08), 팀별 계획서(1주차)

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트 명 | Signal Guard — 실시간 교통신호 상태정보 오류검지 시스템 |
| 사업 원형 | 실시간 교통신호 상태정보 오류검지 시스템 구축 (한국도로교통공단 첨단교통연구처) |
| 목적 | 교차로별 **신호 계획**과 **실시간 신호 상태**를 수집·대조하여 이상을 **검지**하고, 대시보드로 **관제**하며, 정보·계획 통제(필요 시 현장 통제 요청)로 오류를 닫는다. 원본 수신 데이터는 덮어쓰지 않는다. |
| 수업 범위 | 웹 프레임워크 중심 MVP (인천시 데이터, 15주) |
| 팀 | 조찬희 (기획·프론트엔드 / PM), 기경현 (백엔드·데이터) |

### 1.1 한 줄 정의

**수집 → 검지(계획·상태 대조) → 관제(대시보드·이슈) → 통제(정보·계획 수정 / 현장 요청) → 원본 보존**

### 1.2 핵심 가치

| 가치 | 설명 |
|------|------|
| 신뢰성 | 신호 계획 대비 실제 상태의 정합성·일관성 자동 검증 |
| 가시성 | 실시간 상태·오류 등급·수신 지연을 한 화면에서 확인 |
| 대응성 | 해결 가능 오류는 제공 계층을 자동 통제하고, 불가 오류는 이슈·계획 통제·현장 통제 요청으로 닫는다. 원본은 유지 |
| 활용성 | 기간·교차로·오류 유형별 통계와 보고 자료 제공 |

---

## 2. 배경과 문제

### 2.1 현장 문제 (제안요청서)

일부 지자체 주요 교차로에서 운전자에게 **진행 방향 신호등 상태**와 **잔여 신호시간**을 제공한다. 그런데 통신 지연·신호기 오류 등으로 **제공 정보가 실제 신호와 불일치**하거나 잔여시간이 갑자기 바뀌어 운전자 혼란과 민원이 발생한다.

- 연계 현황: 18개 지자체, **2,425개** 교차로
- 수업 검증 대상: 그중 **인천시 727개소** (공개 API로 수집 가능한 범위)

### 2.2 기존 체계

현장 신호제어기 → 이동통신망(LTE/5G) → 경찰청 도시교통정보센터 → 전용회선·Open API → 민간 서비스

본 시스템은 센터에서 **이미 수집·개방 중인 신호 데이터**를 연동해, 오류를 **검지 → 관제 → 통제**하는 계층을 만든다.

통제의 1차 대상은 현장 신호기가 아니라 **운전자·서비스에 나가는 상태정보와 계획**이다. 현장 등화기를 바꿔야 하는 오류는 **현장 통제 요청**으로 남기고, 수업 MVP에서는 신호제어기에 점등·주기 명령을 보내지 않는다. 자동보정·관리자 통제는 제공 계층에만 적용하고 **원본 수신 데이터는 덮어쓰지 않는다.**

### 2.3 제안요청서만으로 부족한 점 (본 문서에서 보완)

제안요청서는 “데이터를 검증하고 이슈로 관리하라”고 요구하지만, **무엇을 무엇과 비교할지**, **오류를 누가 어떻게 닫을지**는 구체적으로 적혀 있지 않다. 본 프로젝트는 아래를 설계 핵심으로 둔다.

1. **신호 계획(기대값)** 과 **신호 상태(실측값)** 를 같은 시각·같은 교차로·같은 방향에서 대조한다.
2. 오류를 분석해 **자동 해결 / 관리자 조치** 로 분기한다.
3. 관리자는 대시보드에서 오류를 확인하고, 정보·계획을 통제하거나 현장 통제 요청을 남긴 뒤 이슈를 종료한다. 원본 수신은 유지한다.

---

## 3. 목표와 범위

### 3.1 목표

1. 교차로 마스터·신호 계획·실시간 신호 상태를 주기적으로 수집·저장한다.
2. 계획 대비 상태, 정합성, 일관성, 수신 품질을 규칙 엔진으로 검증한다.
3. 해결 가능한 오류는 보정 후 정상 데이터로 반영하고, 보정 이력을 남긴다.
4. 해결 불가능한 오류는 즉시 이슈로 등록하고 관리자에게 표시한다.
5. 운영자·관리자가 이슈를 확인·통제·완료할 수 있는 웹 화면을 제공한다.
6. 실시간 모니터링·오류 조회·통계를 대시보드로 시각화한다. (관제)
7. 오류 위치에 따라 정보 통제, 계획 통제, 현장 통제 요청으로 닫는다. 원본은 불변이다.

### 3.2 수업 MVP에 포함

- 인천시 교차로 맵 정보·방향별 잔여시간/점등상태 수집 (실시간 API, 승인·동작 확인)
- 신호 계획(TOD·SIGNALMAP)과 실시간 상태 대조 — 계획은 UTIC 신호 개방 데이터(승인 대기), 승인 전까지는 운영자 입력·주기 추정으로 대체
- 검증 규칙 엔진, 오류 등급, 자동보정/이슈 분기 (검지)
- 웹 대시보드, 오류 목록, 이슈 처리, 통계, 로그인·권한(관리자/운영자) (관제)
- 정보·계획 통제 및 현장 통제 요청 기록. 원본 수신 불변 (통제)

### 3.3 수업 MVP에서 제외 (향후 확장)

- 전국 2,425개소 동시 연계, 센터 내부망 전용 연동
- **현장 신호제어기의 실제 원격 점등·주기 변경**, 현장 출동 워크플로 (통제 요청 기록은 포함)
- 경찰청 메타데이터관리시스템 현행화, 하드웨어 납품
- 고급 ML 이상탐지, 지도 기반 관제(2단계)

### 3.4 제약

실시간 API **일일 트래픽 5,000건** 제한이 있다 (UTIC 계획은 일 1회 수집이라 쿼터와 별개).

- 교차로 전체를 초단위로 폴링하지 않고, **일정 간격 배치 수집**으로 소진을 막는다.
- 수집 공백은 대시보드 **UI 보정 타이머**(잔여시간 1초씩 감소)로 보완한다.
- 우선 수집 대상은 인천시 교차로 중 **표본 집합**(운영 교차로, 오류 다발 교차로)을 두고 확장한다.

---

## 4. 핵심 업무 흐름

```
[UTIC 신호개방 / 실시간 Open API]
        │
        ▼
 ┌──────────────┐     ┌──────────────┐
 │ 신호 계획     │     │ 신호 상태     │
 │ (TOD·맵·      │     │ (잔여시간·    │
 │  현시 구성)   │     │  점등상태)    │
 └──────┬───────┘     └──────┬───────┘
        │                    │
        └─────────┬──────────┘
                  ▼
         정규화 · 저장 (이력)
                  ▼
         검증 규칙 엔진
         (누락·범위·순서·시간역행·
          급변·지연·중복·계획불일치)
                  ▼
            오류 분석·분류
         ┌────────┴────────┐
         ▼                 ▼
  해결 가능 오류        해결 불가 오류
  · 자동 정보 통제      · 이슈 등록 (관제)
  · 제공 계층만 수정    · 정보/계획 통제
  · 원본 보존           · 현장 통제 요청
         └────────┬────────┘
                  ▼
         웹 대시보드 · 통계 · 통제 이력
```

---

## 5. 데이터

대조의 두 축은 **신호 계획(기대값)** 과 **실시간 신호 상태(실측값)** 이다. 전자는 UTIC 신호 개방 데이터, 후자는 공공데이터포털 실시간 API를 쓴다.

### 5.1 수집원 개요

| 구분 | 서비스 | 제공 내용 | 대상 | 갱신 | 현재 상태 |
|------|--------|-----------|------|------|-----------|
| 계획(기대값) | UTIC 신호 개방 데이터 | 교차로 기반정보, TOD(운영·평일·특수일·예약), SIGNALMAP | 인천·대전·대구 | 1일 1회 또는 변경 시 | **승인 대기** |
| 상태(실측값) | 교통안전 신호등 실시간 정보 | 교차로 맵(정적), 8방향 잔여시간·점등상태 | 전국 | 호출 시점 | **승인·동작 확인 완료** |

수업 MVP는 실시간 API로 교차로 목록·좌표·실시간 상태를 먼저 수집하고, UTIC 승인 후 같은 교차로에 TOD·SIGNALMAP을 붙여 계획 대조를 완성한다. 승인 전에는 운영자 입력·주기 추정으로 계획을 대체한다.

제공기관도 실시간 정보는 **처리 지연으로 실제 신호와 차이가 날 수 있으니 참고용**이라고 명시한다. 운영·안전 제어에 직접 쓰지 않는다.

### 5.2 실시간 신호 상태 API (승인 완료)

행정안전부·한국지역정보개발원(KLID)이 공공데이터포털로 제공하는 전국 교차로 신호등 실시간 Open API이다.

| 항목 | 내용 |
|------|------|
| Base URL | `https://apis.data.go.kr/B551982/rti` |
| 포맷 | JSON / XML |
| 인증 | `serviceKey` (공공데이터포털 일반 인증키, Decoding 키) |
| 트래픽 | 개발계정 일 5,000회 |
| 권장 호출 | HTTPS(443) |

호출:

```
https://apis.data.go.kr/B551982/rti/{기능경로}
  ?serviceKey={인증키}
  &pageNo=1
  &numOfRows=1
  &type=json
```

| 파라미터 | 설명 |
|----------|------|
| serviceKey | 포털에서 발급한 Decoding 키 |
| pageNo | 페이지 번호 |
| numOfRows | 한 페이지 건수 |
| type | `json` 또는 `xml` |

**응답 구조 (공통)**

```json
{
  "header": {
    "resultCode": "K0",
    "resultMsg": "NORMAL_SERVICE"
  },
  "body": {
    "totalCount": 4239,
    "pageNo": 1,
    "numOfRows": 1,
    "items": { "item": [ { "...필드..." } ] }
  }
}
```

- `resultCode` **K0** → 정상
- 실시간 API 장애: HTTP 504, `SERVICETIMEOUT_ERROR`
- 연결 자체 실패: SSL/연결 타임아웃

#### 5.2.1 교차로 맵 정보 — `/crsrd_map_info`

교차로의 기본(정적) 정보. 지도에 교차로를 찍고, 실시간 API와 조인할 `crsrdId`를 확보한다.

| 필드 | 의미 | 예시 |
|------|------|------|
| stdgCd | 지자체(법정동) 코드 | 1100000000 |
| lclgvNm | 지자체명 | 서울특별시 |
| crsrdId | 교차로 ID | 1 |
| crsrdNm | 교차로명 | 이촌역앞 |
| mapCtptIntLat / mapCtptIntLot | 위도 / 경도 | 37.523... / 126.971... |
| laneWdth | 차로 폭 | (비어 있을 수 있음) |
| lmtSpdTypeNm / lmtSpd | 제한속도 유형 / 값 | |
| crsrdEngNm | 교차로 영문명 | |
| regDt / totDt | 등록·수집 시각 | |

수업 범위에서는 `lclgvNm`이 인천인 교차로를 표본으로 둔다.

#### 5.2.2 신호제어기 신호잔여시간 — `/tl_drct_info`

신호제어기 기준 8방향별 실시간 신호 상태. 대시보드, 잔여시간 표시, 오류검지(지연·급변·상태 이상)에 쓴다. `crsrdId`로 맵 정보와 조인한다.

**방향 접두어**

| 접두어 | 방향 |
|--------|------|
| nt | 북쪽 |
| et | 동쪽 |
| st | 남쪽 |
| wt | 서쪽 |
| ne / se / sw / nw | 북동 / 남동 / 남서 / 북서 |

**신호 종류 (접미어)**

| 접미어 | 의미 |
|--------|------|
| Bssg | 버스 |
| Bcsg | 자전거 |
| Ltsg | 좌회전 |
| Pdsg | 보행 |
| Stsg | 직진 |
| Utsg | 유턴 |

**값 종류**

| 접미어 | 의미 |
|--------|------|
| ...RmndCs | 잔여시간 (센티초, 1/100초). 예: 18000 → 180초 |
| ...SttsNm | 점등상태명 |

점등상태명:

- `stop-And-Remain` — 정지(적색 계열)
- `protected-Movement-Allowed` — 통행 허용(녹색 계열)

수집 시각은 API 필드와 별도로 **시스템 수신 시각**을 기록한다.

### 5.3 신호 개방 데이터 — UTIC (승인 대기)

안내: [신호 개방 데이터](https://www.utic.go.kr/guide/utisRefSig.do)

인천시·대전시·대구시 교차로에 설치된 온라인 신호제어기의 **신호운영 계획(TOD)** 과 **시그널 맵(SIGNALMAP)** 을 지자체로부터 1일에 한 번 또는 변경 시마다 수집해 제공한다. 교차로 기반정보도 함께 구축되어 있다.

| 항목 | 내용 |
|------|------|
| 제공기관 | 경찰청, 한국도로교통공단 |
| 제공형태 | OPEN API (REST, JSON) |
| 갱신주기 | 일 1회 (또는 변경 시) |
| 분류 | 신호정보 |
| 공개 | 2023.01 / 최신수정 2023.01.16 이후 |
| 샘플 호스트 | `http://tsihub.utic.go.kr/tsi/api/` |

**제공 정보**

| 구분 | 서비스 | 주요 항목 |
|------|--------|-----------|
| 기반(정적) | 교차로 정보 | 지역코드, 교차로번호, 교차로명, 위치(X, Y), 노드 ID |
| 기반(정적) | 교차로 현시구성 정보 | 지역코드, 교차로번호, 맵 번호, A링·B링 현시별 방향설정 코드 |
| 계획(TOD) | 교차로 운영계획 정보 | 계획번호, 인덱스, 운영 시·분, 운영주기, 운영옵셋, A링·B링 1~8 현시 값 |
| 계획(TOD) | 교차로 평일계획 정보 | 계획요일, 계획번호, 수집일시 |
| 계획(TOD) | 교차로 특수일계획 정보 | 특수일 계획일, 계획번호, 수집일시 |
| 계획(TOD) | 교차로 예약계획 정보 | 예약 월·일·요일, 시작·종료 시·분, 예약제어 코드, 수집일시 |
| 동적 | 교차로 시그널맵 정보 | 제어기 형식(2004/2010년식), 링번호(A/B), 등화기 유형(3색/4색), 계획구분(일반제·시차제·보행맵) |

승인 후 명세(코드정의서, 기반정보·계획정보 서비스명세서)에 맞춰 엔드포인트를 확정한다. 샘플 예: `PlanCrossRoadInfoService/getPlanCRRSInfo?serviceKey=인증키&type=json`.

**실시간 API와의 조인**

- UTIC: `REGION_CD`(지역코드), `INT_NO`(교차로번호), `INT_NM`(교차로명), 좌표
- 실시간: `crsrdId`, `crsrdNm`, `lclgvNm`, 위·경도

ID 체계가 다를 수 있으므로 **인천 교차로 매핑 테이블**(이름·좌표 근접 매칭 후 관리자 확정)을 둔다. 매핑되지 않은 교차로는 실시간 모니터링만 하고 계획 대조는 건너뛴다.

**승인 전 대체 (우선순위)**

1. UTIC API (승인되면 1순위)
2. 운영자 등록 계획 (TOD · 현시 순서 · 유지시간)
3. 최근 정상 주기 학습 (오류 없던 N개 사이클에서 추정)
4. 수업용 시드 CSV/JSON

내부 계획 레코드는 출처와 무관하게 아래 항목으로 정규화한다.

| 항목 | 설명 |
|------|------|
| 교차로ID, 방향, 현시종류 | 어느 등화인가 (실시간 필드와 맞춤) |
| TOD 구간 (시작~종료 시각, 요일·특수일) | 언제 이 계획이 유효한가 |
| 현시 순서 | 예: 직진녹 → 좌회전 → 보행 → 전적색 |
| 표준 유지시간(초) | 계획상 초록/보행 유지 시간 (UTIC 현시 값) |
| 사이클 길이(초) | 한 주기 총 시간 (운영주기) |
| 동시 점등 제약 | 대향/충돌 현시가 동시에 녹이면 안 됨 (현시구성·SIGNALMAP) |
| 계획 출처 | UTIC / 수동등록 / 추정 / 시드 |

### 5.4 프로젝트에서 쓰는 방식 (Signal Guard)

1. `/crsrd_map_info`로 교차로 목록·좌표·`crsrdId` 확보 (인천 필터)
2. `/tl_drct_info`로 같은 `crsrdId` 기준 실시간 상태 수집
3. UTIC TOD·SIGNALMAP(또는 대체 계획)을 같은 교차로·시각·방향에 조인
4. 검증 엔진에서 지연·잔여시간 급변·상태 이상·계획 불일치를 탐지
5. 웹 대시보드에 실시간 표시

### 5.5 저장 주기

제안요청서 SFR-003: 일정 주기로 저장하고 일·주·월 단위 관리에 적합해야 한다.

| 계층 | 주기 | 용도 |
|------|------|------|
| 실시간 원시 수신 | `/tl_drct_info` 배치마다 | 원본 보존, 재검증 |
| 교차로 맵 | `/crsrd_map_info` (상대적으로 드물게) | 마스터·좌표 |
| UTIC 계획·맵 | 일 1회 또는 변경 시 | TOD·SIGNALMAP 원본 |
| 정규화 상태 | 수집 배치마다 | 대시보드 현재값 |
| 검증·보정 로그 | 이벤트 발생 시 | 자동수정 추적 |
| 이슈 | 오류 발생/변경 시 | 관리자 처리 |
| 집계 통계 | 시간/일/주/월 배치 | 차트·보고서 (응답 3초 이내를 위해 사전 집계) |

---

## 6. 검증과 오류 처리

### 6.1 검증 관점 (SFR-004 + 팀 설계)

| 관점 | 검사 내용 |
|------|-----------|
| 정보 입력 상태 | 필수 필드 누락, 포맷 오류, 교차로/방향 코드 불일치 |
| 정합성 | 허용 범위 초과, 점등상태 코드 비정상, 잔여시간 음수/과대 |
| 일관성 | 현시 순서 위반, 잔여시간 증가·급변, 시간 역행, 중복 |
| 계획 대조 | 현재 점등·잔여시간이 해당 시각 계획과 어긋남 |
| 교차로 운영상태 | 운영 중이 아닌데 신호가 바뀌거나, 운영 중인데 수신 두절 |
| 수신 품질 | 주기 지연, 부분 누락, 동일 페이로드 반복 |

### 6.2 핵심 규칙 예시

| ID | 규칙 | 기본 판정 |
|----|------|-----------|
| R01 | 필수 필드 누락 (교차로ID, 방향, 점등상태, 잔여시간, 시각) | 자동보정 시도 후 실패 시 이슈 |
| R02 | 잔여시간 범위 (0 이상, 계획 유지시간+여유초 이하) | 경미 이탈은 보정, 큰 이탈은 이슈 |
| R03 | 잔여시간이 수집 간격보다 크게 **증가** | 이슈 (계획 전환이 아니면 비정상) |
| R04 | 잔여시간 **급감** (타이머로 설명 불가) | 이슈 |
| R05 | 현시 순서 위반 (계획 순서와 다른 점등) | 이슈 |
| R06 | 충돌 현시 동시 녹색 (직진 vs 횡단 등) | 이슈 (치명) |
| R07 | 수신 주기 지연 | 지연 길이에 따라 경고 → 이슈 |
| R08 | 중복 데이터 (동일 교차로·시각·페이로드) | 자동 제거 |
| R09 | 타임스탬프 역행 | 정렬 가능하면 보정, 아니면 이슈 |
| R10 | 계획 TOD와 운영시간 불일치 | 이슈 |
| R11 | 점등상태는 적색인데 잔여시간이 초록 구간에 해당 | 이슈 |
| R12 | JSON/코드값 정규화 실패 (별칭, 공백, 대소문자) | 자동 매핑 |

임계값(초, 횟수, 등급)은 설정 테이블로 두고 운영자가 조정한다.

### 6.3 오류 등급

| 등급 | 의미 | 기본 대응 |
|------|------|-----------|
| INFO | 보정 완료, 품질 참고 | 로그만 |
| WARN | 경미, 서비스 가능 | 목록 표시, 반복 시 승격 |
| ERROR | 제공 정보 신뢰 불가 | 이슈 등록, 대시보드 강조 |
| CRITICAL | 충돌 현시·장시간 두절 | 즉시 알림, 최우선 이슈 |

### 6.4 해결 가능 vs 해결 불가

**해결 가능 (시스템이 수정)**  
데이터 품질 문제로, 원본을 해치지 않고 **정규화·보간·중복제거**로 복원할 수 있는 경우.

- 코드/명칭 동의어 매핑, 공백·형식 정리
- 중복 수신 제거
- 수집 공백 동안 잔여시간 1초 단위 감소 (계획·직전 정상값과 모순 없을 때)
- 수신 시각이 수 초 어긋난 경우 정렬
- 단일 필드 결측을 직전 정상 프레임으로 채움 (연속 결측은 불가)

자동 수정 시 반드시 **원본 보존 + 보정 로그**(규칙ID, 보정 전/후, 시각, 신뢰도)를 남긴다. 대시보드에서 “자동보정 N건”으로 보여 준다. 보정 대상은 **제공 계층**이며 원본 수신은 덮어쓰지 않는다.

**해결 불가 (관리자 관제·통제)**  
현장 신호·통신·계획 자체의 문제로, 소프트웨어가 참값을 알 수 없는 경우.

- 계획과 상태가 지속적으로 불일치 → **계획 통제** 또는 정보 제공 차단
- 현시 순서 위반, 충돌 녹색 → **현장 통제 요청** + 제공 차단 (신호기 실제어 없음)
- 잔여시간 급변·역행이 반복
- 교차로 운영상태와 수신 패턴 불일치
- 장시간 미수신
- 자동보정 실패 또는 동일 규칙이 짧은 시간에 반복

이때는 이슈를 만들고 관리자가 **정보 통제 / 계획 통제 / 현장 통제 요청 / 오탐 처리 / 완료** 할 수 있게 한다. 원본 수신은 유지한다.

### 6.5 이슈 생명주기

```
OPEN(신규) → ACK(확인) → IN_PROGRESS(조치중) → RESOLVED(해결) → CLOSED(종료)
                ↘ FALSE_POSITIVE(오탐) ↗
```

이슈 필드: 교차로, 방향, 규칙ID, 등급, 발생시각, 원본/보정값, 추정 원인, 담당자, 조치 내용, 첨부(스크린/로그), 상태.

관리자 수정 권한:

- 잘못된 현재 상태값 수동 정정 (감사 로그 필수)
- 신호 계획(TOD, 현시 순서, 유지시간) 수정 후 재검증
- 해당 교차로 일시 감시 제외(유지보수)
- 오탐으로 규칙을 완화할지는 설정 변경으로만 (코드 하드코딩 금지)

---

## 7. 기능 요구사항

제안요청서 기능 5건을 수업 범위에 맞게 분해한다. ID는 RFP와 대응한다.

### 7.1 SFR-001 시스템 구성

- 웹 앱 + API + DB + 수집 스케줄러로 구성한다.
- 데이터량에 맞는 용량으로 설계하고, 교차로 수를 설정으로 늘릴 수 있게 한다.
- 산출: 시스템 구성도, 수집량·저장량 계산(표본 교차로 수 × 주기 × 일).

### 7.2 SFR-002 신호 데이터 수집

- 교차로 맵·잔여시간 API를 스케줄러가 호출한다 (실시간, 일 5,000건 한도).
- UTIC 신호 개방 데이터는 승인 후 일 1회(또는 변경 시) TOD·SIGNALMAP을 수집한다.
- 누락 방지: 실패 재시도, 부분 실패 교차로 목록, 다음 주기에 우선 수집.
- 검증·저장용 JSON으로 변환한다. 잔여시간 `...RmndCs`는 센티초 → 초로 정규화한다.
- **추가**: 신호 계획 등록 API/화면 (UTIC 적재 + 수동·CSV 보정). 교차로 ID 매핑 관리.

### 7.3 SFR-003 저장 및 관리

- PostgreSQL에 원시/정규화/검증/이슈/통계를 분리 저장한다.
- 일·주·월 집계 배치를 둔다.

### 7.4 SFR-004 검증

- 6장 규칙 엔진을 구현한다.
- 교차로 운영상태·통계/분석을 포함한다.
- **추가**: 계획 대비 상태 대조, 자동보정 파이프라인.

### 7.5 SFR-005 모니터링 프로그램

| 기능 | 내용 |
|------|------|
| 대시보드 | 운영 상태·검증 결과·오류 등급·수신 지연을 한 화면. 차트·그래프 사용 |
| 실시간 오류 내역 | 신규 오류를 목록·배지·(가능하면) 웹소켓으로 전달 |
| 이슈 관리 | 등록, 담당, 상태, 원인, 조치 이력. **관리자 값/계획 수정** |
| 이력·통계 | 유형·기간·교차로별 조회, CSV/출력 |
| 계정·권한 | 등록/수정/삭제, 역할별 메뉴 |

### 7.6 수업에서 추가하는 기능 (RFP에 없음)

| ID | 기능 | 이유 |
|----|------|------|
| EXT-001 | 신호 계획 관리 (UTIC TOD·맵 + 수동 보정) | 대조 기준이 없으면 이상 판정이 불가 |
| EXT-002 | 오류 자동/수동 분기 | “검증만”이 아니라 품질을 회복하는 운영 루프 |
| EXT-003 | 자동보정 이력 조회 | 시스템이 값을 바꿨음을 운영자가 감사 |
| EXT-004 | 관리자 오류 수정·재검증 | 해결 불가 오류를 사람이 닫을 수 있게 |
| EXT-005 | 수집 쿼터 가드 | 일 5,000건 제한 준수 |
| EXT-006 | 검지·관제·통제 루프 | 오류를 닫되 신호기 실제어 없이 정보·계획·현장 요청으로 통제. 원본 불변 |

---

## 8. 사용자와 화면

### 8.1 역할

| 역할 | 권한 |
|------|------|
| 운영자 | 대시보드·오류 조회, 이슈 확인·코멘트, 통계 |
| 관리자 | 운영자 권한 + 이슈 수정/종료, 상태값·계획 수정, 계정·규칙 임계값, 수집 대상 설정 |
| 시스템 | 수집, 검증, 자동보정, 이슈 생성, 집계 |

### 8.2 주요 화면

1. **종합 대시보드**  
   전체/정상/경고/오류 교차로 수, 수신 지연, 자동보정 건수, 미처리 이슈, 시간대 오류 추이, (선택) 인천 지도 위 상태 색상
2. **교차로 상세**  
   8방향 점등·잔여시간, 계획 대비 게이지, 최근 검증 타임라인
3. **오류·이슈**  
   필터(유형·등급·기간·교차로·상태), 상세, 담당 배정, 수정 폼, 이력
4. **신호 계획**  
   UTIC TOD·현시구성·SIGNALMAP 조회, 매핑되지 않은 교차로 수동 등록/수정
5. **통계·리포트**  
   오류율, 다발 교차로, 규칙별 발생, 자동보정 vs 수동조치 비율
6. **설정**  
   사용자, 역할, 수집 주기, 규칙 임계값, API 쿼터 사용량

### 8.3 유스케이스 (요약)

상세 흐름·예외·수용 기준과 사용자 스토리 백로그는 `유스케이스사용자스토리.md`가 정본이다. ID는 요구사항정의서 5.1과 같다.

| UC | 액터 | 설명 |
|----|------|------|
| UC01 | 시스템 | 교차로 맵·실시간 상태를 주기 수집하고 정규화한다 |
| UC02 | 시스템/관리자 | UTIC(또는 수동) 신호 계획을 적재·수정한다 |
| UC03 | 시스템 | 계획과 상태를 대조해 오류를 판정한다 |
| UC04 | 시스템 | 해결 가능 오류를 보정하고 로그를 남긴다 |
| UC05 | 시스템 | 해결 불가 오류를 이슈로 등록한다 |
| UC06 | 운영자 | 대시보드에서 실시간 상태·오류를 본다 |
| UC07 | 관리자 | 이슈를 확인하고 정보·계획 통제 또는 현장 통제 요청 후 재검증한다 |
| UC08 | 운영자 | 기간·교차로별 통계를 조회·내보낸다 |
| UC09 | 관리자 | 계정과 권한을 관리한다 |
| UC10 | 관리자 | 수집 대상·주기·규칙 임계값·쿼터를 설정한다 |
| UC11 | 시스템/관리자 | 통제 이력을 남기고 원본과 제공값을 구분 조회한다 |

### 8.4 사용자 스토리 예시

전체 33건(필수 29)은 `유스케이스사용자스토리.md` 6장을 본다.

- 운영자로서, 한 화면에서 지금 이상이 난 교차로를 보고 싶다. (US-MON-01)
- 시스템으로서, 잔여시간이 1초 단위로만 비면 자동으로 채워 대시보드가 끊기지 않게 하고 싶다. (US-VAL-05)
- 관리자로서, 직진과 보행이 동시에 녹색이면 제공을 막고 현장 통제 요청을 남기고 싶다. (US-CTL-03)
- 관리자로서, 계획이 틀린 교차로는 계획만 고치고 재검증하고 싶다. (US-CTL-02)
- 운영자로서, 이번 주 자동보정과 미해결 이슈 비율을 보고 싶다. (US-RPT-01)

---

## 9. 아키텍처와 기술

### 9.1 논리 구성

```
실시간 Open API ──► 수집 스케줄러 ──► 검증 규칙 엔진 ──► PostgreSQL
UTIC 신호개방(TOD) ─┘        │              │
                             │              ├── 자동보정 로그
                             │              └── 이슈
                             ▼
                        REST API + (선택) WebSocket
                             ▼
                     웹 대시보드 (React 또는 Vue)
```

권장 스택 (팀별 계획서):

| 구분 | 기술 |
|------|------|
| Frontend | React 또는 Vue, Chart.js, WebSocket |
| Backend | Spring Boot 또는 Node.js, REST API |
| Data | PostgreSQL |
| DevOps | Git/GitHub, Docker |

확정 스택은 4~5주차 설계에서 한 가지로 고정한다.

### 9.2 논리 데이터 모델 (초안)

- `intersection` 교차로 마스터 (`crsrdId`, 좌표, 지자체)
- `intersection_id_map` UTIC `INT_NO` ↔ 실시간 `crsrdId`
- `signal_plan` / `signal_plan_phase` 정규화 계획·현시 (출처: UTIC / 수동 / 추정)
- `signal_status_raw` 실시간 원시 수신 (`/tl_drct_info`)
- `signal_status` 정규화 현재/이력 (잔여시간 초 단위)
- `validation_result` 규칙별 판정
- `auto_correction_log` 보정 전후
- `issue` / `issue_history` 이슈와 조치
- `stat_hourly` 등 집계
- `app_user` / `role` 계정

### 9.3 성능 (수업 반영)

- 화면 응답 대부분 **3초 이내** (PER-004)
- 길면 진행 표시, 통계는 사전 집계 테이블 사용
- HTML5 표준 브라우저, 행정안전부 디지털 정부서비스 UI/UX 가이드 취지 준용 (SIR-001)

---

## 10. 제안요청서 대응 (수업 범위)

총 39개 요구 중 **기능·데이터·화면**을 구현하고, 센터 반입·보안 특약·하자보수 등 발주 행정 항목은 **문서·설계로만 대응**한다. 건별 표는 `제안요청서요구사항대응표.md`를 본다.

| ID | 명칭 | 수업 대응 |
|----|------|-----------|
| SFR-001~005 | 구성·수집·저장·검증·모니터링 | **구현** |
| PER-001~004 | 성능 | 표본 규모에서 3초 응답·배치 집계 |
| SIR-001 | Web 표준 | HTML5 반응형 |
| DAR-001~007 | 데이터 표준·구조·값 검증 | 용어/ERD/값 검증 규칙을 프로젝트 표준으로 문서화 |
| TER-001 | 시험운영 | 12~13주 테스트로 대체 |
| SCR-001~007 | 보안 | RBAC, 비밀번호 암호화, 입력 검증, 기본 웹 취약점 점검 (특약·센터 보안은 제외) |
| QUR-001~003 | 품질 | 예외 처리, 복구 절차, 접근성 기본 |
| COR/PMR/PSR | 제약·관리·지원 | 일정·이슈·매뉴얼·시연으로 대체 |

---

## 11. 품질·보안·테스트 (수업)

**완료 기준 (팀별 계획서)**  
핵심 기능 테스트 100% 통과 · 치명/주요 결함 0건 · 매뉴얼로 재설치 및 시연 성공

| 영역 | 내용 |
|------|------|
| 기능 | 요구사항별 테스트 케이스, 규칙 정상/경계/오류값, 주요 흐름 E2E |
| 성능 | 동시 수신 부하, 대시보드 갱신 지연, 오류 발생·복구 |
| 보안 | RBAC, 입력 검증, 비밀번호 해시, 기본 웹 취약점·로그 |
| 운영 | 예외·장애 로그, 백업/복구·배포 절차, 회귀 테스트 |

자동보정은 **원본을 덮어쓰지 않는지**, 관리자 수정은 **감사 로그가 남는지**를 반드시 시험한다.

---

## 12. 팀·일정·산출물

### 12.1 역할

| 이름 | 주 역할 | 세부 |
|------|---------|------|
| 조찬희 | 기획·프론트엔드, PM | 일정·요구사항·화면, 대시보드·이슈·통계 UI, 발표·산출물 |
| 기경현 | 백엔드·데이터 | 수집 API, 검증 엔진, DB, 인증·권한·배포 |

공동: 코드리뷰, 통합테스트, 문서화  
협업: Git 기능 브랜치, PR 상호 리뷰, 주 1회 스프린트, 문서/코드 교차 검수

### 12.2 15주 계획

| 단계 | 주 | 내용 |
|------|----|------|
| 분석 | 1–3 | RFP 분석, 범위, 사용자·유스케이스·백로그 |
| 설계 | 4–5 | 화면·UX·아키텍처, DB·API·검증 규칙 |
| 구현 | 6–11 | 환경·인증, 수집, 저장·검증, 대시보드, 이슈, 통계·권한 통합 |
| 테스트 | 12–13 | 단위·통합·API, 성능·보안·사용성 |
| 안정화 | 14–15 | 결함 수정·배포·매뉴얼, 검수·시연·발표 |

### 12.3 단계별 산출물

- 분석: 본 정의서, 제안요청서요구사항대응표.md, 유스케이스사용자스토리.md, 백로그WBS.md
- 설계: 아키텍처, 화면설계·프로토타입, ERD, API·검증 규칙 명세서
- 구현: 소스, 스키마/시드, 배포 설정, 주차별 기록
- 테스트: 계획·케이스, 결과서, 점검표, 결함 조치
- 안정화: 완료보고, 사용자·운영 매뉴얼, 설치 가이드, 발표·시연

---

## 13. 기대효과

1. **데이터 신뢰성**: 계획과 실측 불일치를 조기에 발견한다.
2. **운영 대응력**: 자동보정으로 단순 품질 이슈를 줄이고, 진짜 이상은 이슈 이력으로 남긴다.
3. **의사결정**: 유형·교차로·기간 통계로 반복 문제 우선순위를 본다.
4. **서비스 품질**: 운전자에게 나가는 신호정보 오류·민원 감소에 기여한다.
5. **실무 역량**: 공공 RFP 분석부터 설계·구현·테스트까지 한 사이클로 경험한다.

> 신뢰할 수 있는 신호 데이터가 안전한 교통정보 서비스의 출발점이다.

---

## 14. 참고

- 제안요청서: 실시간 교통신호 상태정보 오류검지 시스템 구축 (한국도로교통공단, 2025.08)
- 제안요청서요구사항대응표.md (RFP 39건 건별 대응)
- 유스케이스사용자스토리.md (UC01~UC11 상세, 사용자 스토리 33건)
- 백로그WBS.md (제품 백로그·15주 WBS·스프린트)
- 팀별 계획서: 실시간 교통신호 오류검지 시스템.pptx (1주차)
- 실시간 API: 교통안전 신호등 실시간 정보 `https://apis.data.go.kr/B551982/rti` — `/crsrd_map_info`, `/tl_drct_info` (일 5,000건, 승인·동작 확인)
- 신호 계획 API: UTIC 신호 개방 데이터 https://www.utic.go.kr/guide/utisRefSig.do — TOD·SIGNALMAP (인천·대전·대구, **승인 대기**)
- 발주 연락: 첨단교통연구처 이승철, 033-749-5979, leesc@koroad.or.kr (원 사업 참고용)

---

## 15. 결정이 필요한 사항 (다음 설계 주)

설계 4~5주차에 팀에서 확정하면 된다.

1. 프론트(React/Vue), 백엔드(Spring/Node) 최종 선택
2. 인천시 표본 교차로 수와 수집 주기(쿼터 안에서)
3. UTIC 승인 일정과 교차로 ID 매핑(`INT_NO` ↔ `crsrdId`). 승인 전 계획 대체(수동 CSV vs 정상 주기 추정)
4. 실시간 푸시(WebSocket) 1차 포함 여부
5. 지도 시각화 1차 포함 여부
)
